#cloud-config
users:
  - name: ansible
    gecos: Ansible User
    groups: [sudo]
    shell: /bin/bash
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    ssh_authorized_keys:
      - "${ansible_public_key}"

ssh_pwauth: false
disable_root: true
